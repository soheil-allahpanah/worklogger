[package]
name = "common"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["clock", "std"] }
regex = "1.12.3"
serde = { version = "1.0", features = ["derive"] }
jalali-rs = "0.1.1"
chrono-tz = "0.10"

